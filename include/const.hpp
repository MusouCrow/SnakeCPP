// A collection of const.

#ifndef CONST_H
#define CONST_H

const int GRID_SIZE = 32;
const bool GIZMOS = false;

enum Direction {up, down, right, left};
enum KeyState {none, pressed, hold, released};

#endif